/**
 * REMOVED: Supabase configuration - Using Railway PostgreSQL instead
 * All Supabase dependencies have been removed for cleaner Railway PostgreSQL integration
 */

// Export null clients to prevent import errors
export const supabase = null;
export const supabaseAdmin = null;

// REMOVED: All Supabase database types and connection tests
// Using Railway PostgreSQL instead via databaseService.ts